## Tech

Active techs — each maps to `tech/<name>/` and auto-loads its conventions.
Other folders under `tech/` ship as a ready-made library but stay dormant
until added here. Edit this file directly, or run the `init-tech-declaration`
skill to detect and declare them.

- python
- react
